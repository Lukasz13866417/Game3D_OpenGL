package com.example.game3d_opengl.game.player.effects;
public class EffectCategory {
    // TODO can be enum (maybe) or wrapper around int
    // Use them to separate effects from each other if they regard disjoint aspects of player state
}
